# tourism_data_project
Analyzing how peak tourism periods affect customer sentiment in hospitality reviews.

Install helper functions scripts as editable package: in terminal, from project root run: pip install -e .